version: 2
updates: []

# Dependabotのバージョン更新を一時停止している。
# 再開する場合は下記のコメントを外すだけでよい(bundler:/backend、npm:/frontend、
# github-actions:/ の3エコシステムを週次でスキャンする設定)。
#
# updates:
# - package-ecosystem: bundler
#   directory: "/backend"
#   schedule:
#     interval: weekly
#   open-pull-requests-limit: 10
# - package-ecosystem: npm
#   directory: "/frontend"
#   schedule:
#     interval: weekly
#   open-pull-requests-limit: 10
# - package-ecosystem: github-actions
#   directory: "/"
#   schedule:
#     interval: weekly
#   open-pull-requests-limit: 10
